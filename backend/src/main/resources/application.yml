server:
  port: 8081

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://thidasawen03:Thida1234@cluster0.yifhyhw.mongodb.net/smart_campus?appName=Cluster0}
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
        provider:
          google:
            issuer-uri: https://accounts.google.com

logging:
  level:
    root: INFO
    org.springframework.security: INFO
    com.smartcampus: DEBUG

app:
  jwt:
    secret: ${JWT_SECRET:this_is_a_demo_secret_change_me_please_1234567890}
    expiration-ms: 3600000
  cors:
    allowed-origins: "http://localhost:5173"

gemini:
  api:
    key: ${GEMINI_API_KEY:your_gemini_api_key_here}

